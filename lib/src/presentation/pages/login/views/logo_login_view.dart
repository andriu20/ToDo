import 'package:flutter/material.dart';

Widget logoLoginView({required Size size}) => Center(
      child: Image.asset(
        "assets/png/to-do-list.png",
        height: size.height*.2,
      ),
    );
