
  import 'package:flutter/material.dart';

Widget txtLoginView({required Size size}) => Text(
        "Inicio de sesión",
        style: TextStyle(
          fontSize: size.height * .024,
          fontWeight: FontWeight.bold,
        ),
      );